import React from "react";
import { StyleSheet, Text, View, Image, Pressable } from 'react-native';

const Bio = ({ bio }) => {
  return (
    <View>
      <Text>bio editor</Text>
    </View>
  )
}

export default Bio;